{
  // basic data

  // string, number, boolean
  const name: string = "mehedi";
  const roll: number = 98;
  const isHome: boolean = true;

  // null, undefined

  // non-primitive array and objects
  const frinds: string[] = ["taslima", "jahan", "nurmoni", "others"];
  const numbers: number[] = [1, 4, 6, 7, 8, 9];

  const user: {
    readonly name: 'mehedi';
    roll: number;
    class: string;
    age: number;
    isMerried: boolean;
  } = {
    name: "mehedi",
    roll: 23,
    class: "oners",
    age: 24,
    isMerried: false,
  };

}
