{
    // basic data type of typescript

    // string type
    let name : string = 'jhankar  mahbub';
    // number type
    let age : number = 44;
    // boolean type
    let isMerriad : boolean = true;
    // undefined type
    let doing: undefined = undefined;
    // null type
    let others : null = null;

    // array type
    let numbers : number[] = [23, 54,65,32,98,];
    let members: string[] = ['ami', 'tumi', 'se', 'tara'];
    // tuple type --> tuple array type
    let officeNum : [string, number, boolean ]= ['parents academy', 1, true];
}